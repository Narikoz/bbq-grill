# URL Rewriting สำหรับ PHP REST API
Options -MultiViews
RewriteEngine On

# Route ทุก request ไปที่ index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
